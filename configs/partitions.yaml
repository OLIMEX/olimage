partitions:

  root:
    fstab:
      type: 'ext4'
      mount: '/'
      options: 'defaults,noatime,nodiratime,commit=600,errors=remount-ro'
      dump: 0
      passno: 1
    parted:
      type: 'ext4'
      start: '10MiB'
      end: '100%'
