distributions:
  debian:
    components: ['main', 'contrib', 'non-free', 'non-free-firmware']
    recommended: 'bookworm'
    releases:
      - 'bookworm'
    repository: 'http://httpredir.debian.org/debian'
